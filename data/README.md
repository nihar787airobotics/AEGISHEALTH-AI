# Data

The baseline defaults to a **SYNTHETIC** dengue-like daily dataset generated reproducibly in `data/synthetic/`. It is solely an engineering demonstration and must not be interpreted as real surveillance data. The loader can read a replacement CSV with: `date`, `region`, `disease_cases`, `temperature`, `rainfall`, and `humidity`.
